export default function ReactJSX() {
    const content = 'This is About page'
    return (
        // using inline style in JSX
        <div style={{fontSize:'20px', color: 'red'}}>{content}</div>
    )
}